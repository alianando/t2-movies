import 'package:flutter/material.dart';

class SaveMovieButton extends StatelessWidget {
  final int id;
  const SaveMovieButton({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
